# Используем официальный образ Python в качестве базового (тут указывается вплоть до версии)

FROM python:3.11-slim

# Устанавливаем рабочую директорию

WORKDIR /app

# Копируем содержимое папки app в рабочую директорию контейнера

COPY app/ .

# Создаем точку монтирования для данных

VOLUME /app/result

# Устанавливаем переменные окружения для конфигурации Python

ENV PYTHONUNBUFFERED=1

# Запуск скрипта при старте контейнера

CMD ["python", "hello.py"]